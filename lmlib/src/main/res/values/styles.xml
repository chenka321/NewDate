<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!--边框-->
        <item name="android:windowIsFloating">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item>
        <!--半透明-->
        <item name="android:windowNoTitle">true</item>
        <!--无标题-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--背景透明-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--模糊-->
    </style>

    <style name="RoundRectFrameLayout">
        <item name="roundRadius">5dp</item>
        <item name="roundStrokeColor">#0000</item>
        <item name="roundStrokeWidth">0.5dp</item>
        <item name="android:background">#0000</item>
    </style>


    <style name="PopDialog" parent="@android:style/Theme">
        <item name="android:windowNoTitle">true</item>
        <!-- 设置title -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <!-- 设置边框 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 设置半透明 -->
        <item name="android:windowFullscreen">false</item>
        <!-- 设置全屏 -->
        <item name="@android:windowEnterAnimation">@anim/popup_up</item>
        <!-- 进入时的动画 -->
        <item name="@android:windowExitAnimation">@anim/popup_down</item>
        <!-- 退出时的动画 -->
    </style>

    <style name="ImageDialog" parent="@android:style/Theme">
        <item name="android:windowNoTitle">true</item>
        <!-- 设置title -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <!-- 设置边框 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 设置半透明 -->
        <item name="android:windowFullscreen">true</item>
        <!-- 设置全屏 -->
        <item name="@android:windowEnterAnimation">@anim/popup_up</item>
        <!-- 进入时的动画 -->
        <item name="@android:windowExitAnimation">@anim/popup_down</item>
        <!-- 退出时的动画 -->
    </style>

</resources>